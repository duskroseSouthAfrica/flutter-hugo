name: hugo
description: "Hugo app — Steps 1-3 prototype."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  record: ^5.1.2
  path_provider: ^2.1.2
  permission_handler: ^11.3.1
  llama_flutter_android: ^0.1.1

dependency_overrides:
  record_linux: ^1.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/models/phi-3.5-mini-q4.gguf
